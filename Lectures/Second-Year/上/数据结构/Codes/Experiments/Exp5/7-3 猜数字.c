#include<stdio.h>
#include<string.h>

//定义定长字符串
#define MaxLen 10000
typedef struct {
	char ch[MaxLen];
	int len;
} SString;

int CalP(SString s){
	
}

int CalQ(SString s){
	
}


int main(){
	SString s;
	
	gets(s.ch);
	int X = CalP(s)+CalQ(s);
	printf("%d",X);
	
	return 0;
}
